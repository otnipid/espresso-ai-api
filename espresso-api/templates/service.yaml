# templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: {{ include "espresso-api.fullname" . }}
  labels:
    {{- include "espresso-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "espresso-api.selectorLabels" . | nindent 4 }}